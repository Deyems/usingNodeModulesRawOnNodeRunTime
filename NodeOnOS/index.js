const os = require('os');

//We can use various methods on the os like:

/*
os.cpus();
os.networkInterfaces().en0.map(i => i.address)
os.freemem();
os.type();
os.release();
os.userInfo();
os.constants.signals;
*/
